# 上游同步分析记录：simonlin1212/Vibe-Research main（→ v0.3.1，bug 修复轮）

- 分析日期：2026-08-11
- 对比范围：`origin/main`(FLIER001, 862028b) vs `upstream/main`(simonlin1212, d8c80d4)
- 上次分析基准：3eae02d（v0.3.0）；本次增量 = 3eae02d..d8c80d4，共 7 个提交
- 本地自上次分析后前进 3 个提交：49243a3（v0.3.1 用户体系）、1c5d4cb（宏观/组合迭代）、862028b（宏观/持仓/板块/基金完善）
- 本地工作区：干净
- 上游本轮性质：**纯 bug 修复 + 版本号治理**（对应 issue #20/#26/#27/#28 与 codex 复审轮），无新功能

## 上游增量提交与改动文件

3eae02d..d8c80d4 共触及：CHANGELOG.md(新)、README/README_en、backend/app.py、chat.py、
gstock.py、mcp_server.py、tools.py、version.py(新)、backend/tests×3(新)、
frontend/package.json、Layout.tsx、AskAiButton.tsx、DailyReview.tsx、Debate.tsx、Intel.tsx、Notes.tsx。

## 逐条结论

| # | 提交 | 内容 | 结论 | 与本地关系 |
|---|---|---|---|---|
| 1 | 50346be | version.py：版本号从 frontend/package.json 单一来源读取（#20） | **采纳** | 本地三处硬编码仍在且已漂移（app.py FastAPI 0.3.0 / health 0.3.1 / mcp_server 0.3.0，package.json 已 0.3.1），同一问题真实存在；version.py 新文件零冲突，app.py/mcp_server 替换点与本地改动不同区 |
| 2 | 3ec179a | MCP serverInfo 也走同一版本来源 | **采纳**（随 #1） | mcp_server.py 本地该处未改，干净 |
| 3 | be99807 | version.py 警告走 stderr 不污染 MCP stdout | **采纳**（随 #1，取最终态 version.py 即含） | 本地 macOS 同样受益（仅后端部署读不到 package.json 的场景） |
| 4 | 9489f9d | #28 turnover 字段名修复 + #27 MCP UTF-8 stdio + #26 搜索备用端点 + CHANGELOG/README 更新日志入口 + 版本号 0.3.1 | **采纳**（前端版本号除外） | ① tools.py turnover：本地是同一份错误字段名（turnover/changePct），bug 原样存在，行级干净；② mcp_server _force_utf8_stdio：本地未改该文件；③ gstock 搜索：本地仍是单端点 + `except: return None`，clean apply，SearchUnavailable 会自然经 app.py 通用 502 与 tools.py exec_tool 兜底，中文报错信息对用户反而更友好；④ CHANGELOG.md 为新文件直接引入；⑤ package.json 上游 0.3.1 与本地 49243a3 自升的 0.3.1 巧合撞号，版本号行不动；README 的更新日志入口可加 |
| 5 | 8864304 | 搜索端点校验响应结构才收手（#26 复审补） | **采纳**（随 #4，取最终态 _search 即含） | 同上 |
| 6 | 3f060a3 | 畸形 payload 校验到底 + turnover 补回 float_cap | **采纳**（随 #4，取 tools.py/_search 最终态即含） | 同上 |
| 7 | d8c80d4 | chat.py baseURL 白名单加 /v4（智谱端点不再被误补 /v1）+ 前端 prose-invert 改 dark: 变体 + AskAiButton 版本注释 | **采纳**（chat.py）；**不适用**（Debate.tsx）；prose 替换按本地实际文件执行 | ① chat.py `_check_base_url`/`_resolve_base` 两处 endswith 元组本地与上游同文，直接替换；② 上游改了 Debate/Intel 的 prose-invert，但本地 Debate.tsx 已删除、Intel.tsx 已重构（无该行），跳过；本地实际有 5 处 prose-invert（AskAiButton、Notes×2、DailyReview、DeepAnalysisButton），上游没碰的 DeepAnalysisButton 是本地自有文件，可顺手统一；③ AskAiButton 该行本地与上游同文（上次合并带入），可换 |

## 汇总：建议动作

**采纳（全部为 bug 修复/治理，本地同款问题逐条核实存在）**

- backend/version.py（新文件）+ app.py/mcp_server.py 接同源版本号——顺带治好本地 0.3.0/0.3.1 漂移
- backend/tools.py turnover 字段名对齐（price/pct/amount/mcap/float_cap/industry）
- backend/gstock.py 搜索健壮性：SearchUnavailable + 备用端点 + 结构校验（取上游最终态整段替换本地 _search/resolve_symbol 前缀部分）
- backend/mcp_server.py _force_utf8_stdio + SERVER_INFO 同源
- backend/chat.py baseURL 白名单 +/v4（两处）
- 前端 5 处 `prose-invert` → `dark:prose-invert`（AskAiButton、Notes×2、DailyReview、DeepAnalysisButton）
- CHANGELOG.md 引入（标注本仓库维护口径）+ README/README_en 更新日志入口
- backend/tests/ 三个新测试文件原样引入

**不采纳 / 跳过**

- frontend/package.json 版本号：本地已是 0.3.1（49243a3 自升），不再动
- Debate.tsx / Intel.tsx 的 prose 改动：文件在本地已删/已重构，目标行不存在

**零冲突确认**

- 本地三条新提交的功能面（宏观/基金/黄金/板块/用户体系）与上游本轮修复的文件交集仅限 app.py、tools.py、gstock.py、chat.py 的不同区块，无文本级冲突

## 验证建议（合并后）

1. `cd frontend && npm run build` + `npm test`
2. `cd backend && .venv/bin/python -m pytest tests/ -q -m "not live"`（含新引入的 3 个测试文件）
3. 实测：`/api/health` 版本号应为 0.3.1 且与 package.json 同源；`query_market scope=turnover` 字段有值
4. launchd 常驻后端需 kickstart 重启加载新代码

---

## 执行结果（2026-08-11 当日执行）

按「汇总：建议动作」全部执行，用户确认。

已合入：
- backend/version.py（新）+ app.py/mcp_server.py 版本号同源——顺带治好本地 0.3.0/0.3.1 三处漂移
- backend/tools.py turnover 字段名对齐（含 float_cap）
- backend/gstock.py 搜索健壮性整段替换：SearchUnavailable + searchadapter 备用端点 + 结构校验（本地 market-hours 插入块在函数外，未受影响）
- backend/mcp_server.py _force_utf8_stdio + SERVER_INFO 同源
- backend/chat.py baseURL 白名单 +/v4（两处）
- 前端 5 处 prose-invert → dark:prose-invert（AskAiButton、Notes×2、DailyReview、DeepAnalysisButton；上游的 Debate/Intel 目标行本地不存在，跳过）
- CHANGELOG.md 引入 + README/README_en 更新日志入口
- backend/tests/ 三个新测试文件原样引入（test_gstock_search / test_mcp_stdio_encoding / test_version_source）

未采纳：frontend/package.json 版本号（本地 49243a3 已升 0.3.1，与上游巧合撞号，不动）。

验证记录：
- 后端 `pytest -m "not live"`：148 passed（含新引入测试），13 deselected(live)
- 后端 live 全量 `pytest -m live`：13 passed（联网 57s，含 hk_cashflow）
- 前端 `npm run build` 通过、`npm test` 全绿
- launchd 后端已 kickstart 重启：`/api/health` 返回 0.3.1（与 package.json 同源）；`/api/global/hk/cashflow?symbol=00700` 正常

遗留 / 注意：
- SearchUnavailable 现经 app.py 通用兜底表现为 502「证券搜索接口暂时不可用…」，与旧 404「未找到」语义区分（预期行为）
- CHANGELOG.md 从此随本仓库维护，后续发版记得同步更新
