# 上游同步分析记录：simonlin1212/Vibe-Research main（→ v0.3.0）

- 分析日期：2026-07-31
- 对比范围：`origin/main`(FLIER001, 686dd72) vs `upstream/main`(simonlin1212, 3eae02d)
- 共同祖先：37a23f8（docs: 数据源口径对齐内嵌副本真实版本）
- 上游领先 18 个提交（37a23f8..3eae02d），新 tag：v0.3.0
- 本地领先上游 5 个提交 + 3 个未提交文件（backend/market.py、DailyReview.tsx、Liquidity.tsx）
- 参考：<https://github.com/FLIER001/Market-Workbench/compare/main...simonlin1212:Vibe-Research:main>

## 本地改动清单（判断冲突的基准）

| 本地提交 | 内容 | 触及的与上游重叠文件 |
|---|---|---|
| 3d67f83 | 券商式股票搜索自动提示 | StockSearchInput.tsx、api.ts |
| 551a4bb | 资讯雷达今日要点增强 | Intel.tsx、api.ts、backend/app.py、radar 相关 |
| b405643 | 板块评分与分组自选 | backend/sector_scores.py(新)、app.py、api.ts、Sectors/Watchlist 等 |
| e62493e | 板块评分、自选与个股K线完善 | StockData.tsx、api.ts、AskAiButton.tsx(接 backgroundTasks)、DailyReview 等 |
| 686dd72 | 资金供给独立页 + 分时图 + 每日复盘重构 | Liquidity.tsx(新页)、backend/market.py、AskAiButton.tsx、DailyReview 等 |
| 未提交 | market.py 主力动量指数 60/40 加权 + 实时值；DailyReview/Liquidity 缓存 key 升 v2 | 与上游文件**无重叠** |

AskAiButton 调用点（本地均传 `taskId`）：DailyReview(`reviewKey`)、Portfolio(`portfolio`)、
SectorDetail(`sector:{key}`)、Watchlist(`watchlist-{collection}`)、StockData(`stock:{code}`)。

## 逐条结论

| # | 提交 | 内容 | 结论 | 与本地关系 |
|---|---|---|---|---|
| 1 | a8ebc5c | 港股现金流量表（初版，东财 RPT_HKSK_FN_CASHFLOW） | 采纳（已被 9c96ffc/6e7ba1d 修正，取最终态） | app.py/gstock.py 本地均改过但不同区域；StockData.tsx/api.ts 同文件异区，需手工合并 |
| 2 | e9bfe9f | Ask AI 回答用 ReactMarkdown 渲染成结构化笔记 | 采纳（需在 backgroundTasks 版组件上重写接入） | AskAiButton.tsx 本地已重写为后台任务版，**文本冲突**，语义可融合 |
| 3 | fc972c6 | merge pr17（Markdown 渲染合入） | 随 #2 | 同上 |
| 4 | 9c96ffc | 港股现金流（社区 PR #14 版）+ query_hk_cashflow 工具 | 采纳 | chat.py、tools.py 本地未改，干净 |
| 5 | e37ae26 | 内嵌 a-stock-data 副本同步至 v3.6.0（端点 44→47） | 采纳 | 本地上次同步到 v3.5.1（4faa485/729b3bf），直接快进 |
| 6 | 3511e23 | Ask AI 对话持久化 + 清空入口 (#19) | **不采纳**（用户决策） | 本地 686dd72 已有 backgroundTasks 持久化通道，两套机制重叠，照搬会双写/串台；仅其衍生的两条通用 bug 修复逻辑单独吸收（见 #10/#13） |
| 7 | aff418e | 持久化两处数据完整性修复 | **不采纳**（随 #6） | 同上 |
| 8 | a4a1a35 | 对话归属原子 state；换 key 中止流式 | **不采纳**（随 #6） | 本地流式中止由 backgroundTasks 的 AbortController 托管，无需上游的原子 state 方案 |
| 9 | c2649fd | 中止清理逻辑补身份校验 | **不采纳**（随 #6） | 同上 |
| 10 | b8167e3 | 空气泡三态判断；个股页 scopeKey 用已解析代码 | 不采纳持久化框架；**已采纳**「key 只用已解析代码」 | 本地 StockData 原 `taskId=stock:{gstock?.code \|\| val?.code \|\| code}` 有同款问题（输入框 code 兜底会随打字变 key），已改为只用已解析代码；三态判断属持久化框架，不引入 |
| 11 | 22b6333 | 流式回答创建即标 partial，成功才摘除 | **不采纳**（随 #6） | 本地 backgroundTasks 通道下「半截回答会落盘并进 history」的隐患仍在，已在遗留项记录，日后有反馈再单独处理 |
| 12 | 7a87124 | 未完成轮次整轮丢弃 | **不采纳**（随 #6） | 同上，见遗留项 |
| 13 | 393a837 | 空气泡连同提问一起移除 | **已采纳**（通用 bug 修复逻辑） | 本地 catch 原来只删空 assistant 泡、留下孤立 user，已修为整轮移除；与持久化机制无关 |
| 14 | aab3d3d | revert 误提交的 SectorDetail/sectors.json WIP | **不适用** | 该 WIP 是上游作者本地改动，与本仓库的 SectorDetail/sectors.json（20 行业研究数据）无关，跳过 |
| 15 | 432a518 | （中止时半截回答标 partial，不落盘不进 history） | **不采纳**（随 #6） | 同 #11 |
| 16 | 6e7ba1d | 港股现金流服务端按科目码过滤，避免截断 | 采纳 | gstock.py 追加函数内部修改，跟随 #4 |
| 17 | 7904cec | 版本号 0.3.0 + scopeKey 约束写进 prop 文档 | **已采纳**版本号；scopeKey 文档不采纳 | scopeKey 是上游持久化 prop，本地用 taskId，文档约束不适用 |
| 18 | 3eae02d | v0.3.0 merge | 作为整体参考 | — |

## 汇总：建议动作

**可直接采纳（无冲突或仅同文件异区，机械合并即可）**

- 港股现金流量表全链：`backend/gstock.py::hk_cashflow`（含 6e7ba1d 过滤修复）、`backend/app.py` /api/global/hk/cashflow、`backend/tools.py` query_hk_cashflow、`backend/chat.py` 提示词一行、`backend/tests/test_live.py` 用例、`api.ts` HkCashflow 类型 + `api.hkCashflow`、StockData.tsx 现金流卡片。
- a-stock-data 内嵌副本 v3.5.1 → v3.6.0（含 README/README_en/backend README 的版本号，注意 README 同区但改的是同一行版本号，取 v3.6.0）。
- frontend/package.json：version 0.3.0 + `npm test` 脚本；react-markdown/remark-gfm 依赖本地已存在，lock 不需动。
- frontend/tests/ask-ai-markdown.test.mjs（断言与本地版组件兼容，原样引入；ask-ai-persistence.test.mjs 属持久化测试，不引入）。

**需适配后采纳（与本地 686dd72/e62493e 的 AskAiButton 后台任务版同文件，需手工移植）**

- Ask AI Markdown 渲染：在本地版组件的 assistant 气泡处加 `<ReactMarkdown remarkPlugins={[remarkGfm]}>`，改动小。
- 两条与持久化机制无关的通用 bug 修复逻辑（从持久化提交中剥离）：
  1. 出错/中止时空气泡连同它的 user 提问整轮移除（393a837，本地 catch 原只删空泡留孤立 user）；
  2. StockData 的 key 只用已解析代码（b8167e3，本地原以输入框 code 兜底，打字时 key 会变）。

**不采纳**

- 对话持久化整套（3511e23 + aff418e/a4a1a35/c2649fd/22b6333/7a87124/432a518，**用户决策**）：本地 686dd72 已把 AskAiButton 重构成 backgroundTasks 后台任务版，对话跨页面存活与恢复由该通道负责；上游 `pathname+scopeKey` 存 localStorage 是第二套持久化，叠加会双写、串台。其 partial/整轮丢弃/清空按钮/消息上限等完整性规则随之不引入，相关隐患记入遗留项。
- aab3d3d（上游私有 WIP 的 revert，与本仓库无关）。

**与本地完全无关、零冲突**

- 本地未提交的 market.py 动量指数、DailyReview/Liquidity 缓存 key v2：上游未触及这三个文件，不受影响。

## 验证建议（合并后）

1. `cd frontend && npm run build`（tsc 过 AskAiButton/StockData 合并结果）
2. `npm test`（两个 ask-ai 测试，需先按本地版组件调整断言）
3. 后端 `python -m pytest backend/tests/test_pure.py`；live 测试需联网另跑
4. 起服务实测：个股页输 00700 看现金流卡片；问 AI 流式回答中途换标的，检查对话不串台、半截回答不落盘
5. launchd 常驻服务需重启后端才加载新端点

## 下次流程提示

1. `git fetch upstream` → `git log --oneline origin/main..upstream/main` 取增量
2. 更新本文件日期/范围或新建同日文件；本地基准以上次分析时的 origin/main 为准
3. 重叠文件重点复核：AskAiButton.tsx、StockData.tsx、api.ts、app.py、gstock.py、a-stock-data/

---

## 执行结果（2026-07-31 当日执行）

用户决策：**不采纳上游「Ask AI 对话持久化」**（3511e23 及其 8 个后续 fix、ask-ai-persistence.test.mjs 均未引入），其余按上表执行。

已合入：
- 港股现金流量表全链（backend/gstock.py::hk_cashflow、/api/global/hk/cashflow、query_hk_cashflow 工具、chat.py 提示词、test_live.py 用例、api.ts 类型、StockData.tsx 现金流卡片）
- a-stock-data 内嵌副本 v3.5.1 → v3.6.0 + 三个 README 版本号（47 端点）
- frontend/package.json：version 0.3.0 + `npm test` 脚本
- Ask AI 回答 Markdown 渲染（AskAiButton.tsx 加 ReactMarkdown + remark-gfm）
- 本地同款 bug 修复：出错/中止时空气泡连同提问整轮移除（393a837 逻辑）、StockData taskId 只用已解析代码（b8167e3 逻辑）
- frontend/tests/ask-ai-markdown.test.mjs 原样引入（断言与本地版组件兼容）

验证记录：
- `npm run build`（tsc -b + vite build）通过
- `npm test` 2/2 通过
- 后端 pytest 84 passed, 12 deselected(live)
- live 测试 test_hk_cashflow_shape 通过
- launchd 后端已 `kickstart -k` 重启，实测 `GET /api/global/hk/cashflow?symbol=00700` 返回腾讯 2026Q1 起多期现金流
- 前端 dev server 为 Vite HMR，无需重启

未合入 / 遗留：
- 上游对话持久化整套（按用户决策不采纳）；partial/整轮丢弃/清空按钮等规则**未引入**——本地 backgroundTasks 通道下「半截回答会落盘并进后续 history」的隐患仍在，若日后出现相关反馈再单独处理
- 本地未提交改动（market.py 动量指数、DailyReview/Liquidity 缓存 key v2）保持原样，未受影响
