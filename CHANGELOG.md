# Changelog

本项目的版本号唯一来源是 `frontend/package.json`；后端 HTTP API、`/api/health`、
前端界面与 MCP `serverInfo` 全部从它读取（见 `backend/version.py`）。

## 未发布（去 vibe 命名 · 统一为 Market Workbench）

- **数据目录更名**：`~/.vibe-research/` → `~/.market-workbench/`（账号库、持仓账本、
  研报与全部缓存快照的根目录）。这是本次唯一会动到磁盘数据的改动，旧目录需手工
  `mv ~/.vibe-research ~/.market-workbench` 迁移。
- **环境变量前缀更名**：`VR_*` → `MW_*`（`MW_DATA_DIR`、`MW_API_KEY`、
  `MW_ALLOW_ORIGINS`、`MW_ALLOW_REGISTRATION`、`MW_DEEP_ANALYSIS_*`、`MW_EIA_API_KEY`、
  `MW_PULSE_LLM_*`、`MW_DATA_PROXY`、`MW_MEM_WATCHDOG`、`MW_HOST`/`MW_PORT` 等）。
  升级需同步更新 launchd plist 与任何外部脚本里的变量名。
- **launchd 标识更名**：`com.vibe-research.app` / `-restart` → `com.market-workbench.app` /
  `-restart`，日志改到 `/private/tmp/market-workbench-app.log`；plist 文件名同步更名。
- **浏览器导出格式更名**：`vibe-research-user-data` → `market-workbench-user-data`，
  备份文件名前缀同步改为 `market-workbench-backup-`（旧备份 JSON 需改字符串后才能导入）。
- 其余零散标识（User-Agent、CLI 临时目录前缀、迁移日志前缀、README/docs 示例）一并去 vibe。

## 未发布（统一启动 · 单进程）

- **前后端统一为单进程启动**：后端静态托管 `frontend/dist`（`/assets` + SPA 回退路由），
  日常使用只需 `./run.sh` 一个命令、一个进程、一个端口（8900）。省掉常驻的
  vite dev server（实测 node+esbuild 约 250MB 常驻内存），静态文件由 FileResponse
  直接服务，开销忽略不计。资产仍走 GZip（1.1MB 主包 → 320KB）。
- SPA 回退路由放在 app.py 末尾（catch-all 必须在全部 API 路由之后注册）；
  `/api/*` 未命中时仍返回 JSON 404，不回退到 index.html（前端 api 客户端靠状态码判错）。
- `run.sh`：dist 缺失或源码比 dist 新才重新 `npm run build`（增量），然后单进程启动 uvicorn；
  `MW_HOST`/`MW_PORT` 可覆盖。前端热更新开发仍可另开 `cd frontend && npm run dev`（5899，
  `/api` 代理到 8900），与生产模式互不影响。
- launchd 两个 plist（backend uvicorn + frontend vite）合并为 `com.market-workbench.app.plist`
  单服务；环境变量（MW_DEEP_ANALYSIS_KEY）与 KeepAlive/RunAtLoad 语义不变。
- README / docs/getting-started.md 快速开始改为统一入口。

## 未发布（2026-09-15 择时配置 · 双图联动回放）

- 择时配置页新增「择时回放」模块：**上方 = 择时分逐日回放曲线，下方 = 全A指数走势**，
  两张图共用同一条日期横轴。悬停在任一张图上都会在**两图同时**画出一条垂直虚线参考线
  （被悬停的那张额外给水平虚线与定位点），顶部读数行同步显示该交易日的两条数值，
  横轴下方弹出该交易日日期气泡。
- 版式：与首屏结论卡并排一行、**各占 1/2**（`lg:grid-cols-2`，<lg 堆叠单列），
  用 `items-start` 各取自然高度；原全宽版式下两卡高差 176px，把绘图区高度做成可调
  （`timingHeight` / `benchHeight`，刻度条数随高度自适应）并收窄口径脚注后压到 **12px**。
- 对齐实现：两图放在**同一个 SVG**、共用一套 x 映射与同一条日期序列（260 个交易日），
  而不是两个各自算宽度的组件——后者在滚动条/圆整误差下会让竖虚线错开 1px 级。
  缺当日值处断开而不前向填充，不臆造走势。
- 修掉一处口径缺陷：择时分逐日回放原先混入**非交易日**（6 个周末点，来自日度宏观快照
  在周末照常续接），这些点当天只有部分成分有值、按已覆盖权重归一后与交易日不同口径
  （实测 2025-09-28 周日 60.7，夹在周五 71.7 与周一 76.2 之间），既扭曲曲线又让下联图
  横轴错位。现按中证全指日K取交易日历过滤，并让回放尾点落在「最近交易日」而非快照运行日。
  修复后两条序列**完全同轴**（同起止日、同为 260 点）。
- 修掉一处布局缺陷：`grid` 只写 `lg:grid-cols-2` 时，<lg 走**隐式列**、按 min-content
  布局不随容器收缩，而图表 SVG 首帧宽度取的是初始 state，两者互相放大把整行顶爆
  （实测 760 视口下页面横向溢出 65px、卡片被撑到 794px）。补 `grid-cols-1`
  并把 SVG 初始宽度改为最小可用宽 320 后消除。
- 后端：`timing.benchmark`（全A指数 = 中证全指 000985 日收盘，裁剪到回放区间）；
  新增 `_trading_days()` / `_last_trading_day()`；`_index_points` 改为「一次取全、
  按需切片」缓存（原按 `days` 缓存，趋势模块的 320 会把回放基准需要的 260 截短）。
  MCP `query_timing_allocation` 增补 `benchmark` 压缩摘要（起止点 + 区间涨跌）。
- 前端：新增 `components/allocation/TimingReplayChart.tsx`；首屏结论卡里与新品重复的
  小 sparkline 与其「近5日」徽标迁入新模块头部（结论卡由两栏改单栏）。
  核对手法：无头壳派发 `mousemove` 后断言竖虚线 `x1==x2` 且 `y1` 落在上图内、`y2` 落在
  下图之外（贯穿两图）、日期气泡与横轴落点交易日一致、读数行等于该日真实数值。

## 未发布（2026-09-14 油价页 · WTI 暗盘涨跌图）

- 油价页新增「WTI 暗盘」卡：**分时涨跌图**（价格线 + 双边涨跌幅坐标轴 + 红绿量柱 +
  均价线，x 轴铺满当日 00:00–24:00），数据源为 Hyperliquid 永续 `xyz:CL`
  （trade.xyz 部署，1 张 = 1 桶，**7×24 含周末连续报价**）。
- 补的是传统外盘的空档：Brent/WTI（腾讯 `hf_OIL`/`hf_CL`、NYMEX）周末、每日结算窗口
  与假期均无报价，而 OPEC+ 表态与中东局势常在周末发酵、开盘跳空即已定价；暗盘时段
  同样出价，可提前看到预期变化。范式与黄金页 PAXG-USD 暗盘一致。
- 口径：昨收取**北京午夜前最后一根 1 分钟收盘**（与分时图基准同源，非 Hyperliquid
  UTC 日界的 `prevDayPx`）；最新价取永续 `midPx` 并覆盖分时末点，保证图尾与卡片一致；
  成交量为当日**累计**桶数（前端 MinuteChart 以累计量差分画量柱）；另附持仓量、
  24h 名义成交额与资金费率（**1 小时**费率，年化 = 费率 × 24 × 365）。
- 后端新增 `oil.wti_hyper_spot()`（20 秒 ticker / 60 秒分时双档缓存，失败回退上次成功
  结果并打 `stale`）与 `GET /api/oil/wti`；数据源健康页新增 Hyperliquid 探针。
- 前端新增 `api.oilWtiHyper()`；暗盘行情并入石油页 20 秒轮询（后台标签页暂停），并写入
  「问 AI」上下文。新增纯函数测试 4 例（分时解析、昨收与末点对齐及累计量、stale 回退、
  无历史空骨架）。
- 油价页「实时行情」卡改为**暗盘主视图 + 三个外盘品种并列**：WTI 暗盘（Hyperliquid 7×24 分时图
  + 持仓 / 24h 名义成交 / 资金费率）占主位；下方 **布伦特原油 / 纽约原油 / 美国天然气**
  三卡并列（各 1/3），每卡含实时报价、当日区间位置条与一条**日K迷你折线**
  （新浪全球期货近 250 个交易日）。外盘周末与每日结算窗口无报价，两种口径互补——
  休市时段的价格变化看暗盘。新增 `GET /api/oil/futures-hist`（三品种日K打包，1 小时缓存，
  单品种失败只让该条为空）。原三列报价小卡与独立暗盘卡合并为此面板，页面首行由三列改为
  两列（评分卡 + AI 解读）。

## 未发布（2026-09-14 事件分析 · 增持口径与来源完善）

- 「事件分析 · 高管/股东增持」表列改为：计划开始日期 / 计划结束日期 / 最新增持日期 /
  已增持金额 / 计划增持金额（原「增持时间」区间下移到展开明细的「披露区间」列）。
- 增持计划解析重写：窗口按「显式起止区间 → 自(于)X年X月X日起 N 个月 → 自公告披露/
  首次增持之日起 N 个月 → 实施期限 N 个月 → 单点结束日」依次尝试，支持中文数字
  （"六个月内"）与"（含）"式金额下限，全部候选校验「结束日不早于公告日」，修掉把
  正文里"截至某历史日期持股…"错当计划期限的问题（实测 30 条计划里 6 条期限错误）；
  同文多条候选取起点最贴近公告日的一条（续做公告常把前次计划一并写进正文）。
- 计划【设立】公告标题新增过滤（进展/结果/时间过半/取消/终止/延期等后续披露不再被
  当作计划公告）。实测计划窗口解析覆盖 30→57/58，金额下限 23→46/58。
- 新增「最新增持日期」口径：股东类取披露的交易截止/成交日、高管类取持股变动日
  （`buy_date`），公告日仅兜底；短窗口筛选锚点不变。新增「已增持金额」按计划开始日
  累计，起始日早于回看期（35 天）时置 `amount_done_partial`，页面显示"≥"。
- 所有日期统一 YYYY-MM-DD（页面 YYYY/MM/DD）：数据层 `_d/_iso` 丢弃残缺日期，
  修掉价格图 tooltip 里 `slice(5)` 导致日期丢年份（"09/11"）的问题。
- 新增数据来源清单（随接口返回）：高管持股变动明细 `RPT_EXECUTIVE_HOLD_DETAILS`、
  股东增减持 `RPT_SHARE_HOLDER_INCREASE`、增持计划公告（标题+正文解析），页面
  「数据来源」区逐项给出数据集名、覆盖字段、原始出处与入口链接；计划日期/金额单元格
  可直接点开对应计划公告原文。
- 快照与前端缓存键升版本（后端 `holder_increase:raw:v2` / 快照 v2 文件名、前端
  `event:holder-increase:v2`），旧结构不兼容值自动失效。
- 新增联网 shape 冒烟测 `tests/test_live.py::test_holder_increase_pipeline_shape`
  （日期格式、成交日口径、计划窗口字段），纳入既有 `pytest -m live` 升级核对流程。
- 计划解析覆盖面提高：计划公告解析上限 40→90 只并加 45s 时间预算（整轮重拉实测约 50s；
  计划覆盖 30→44 只，行级计划覆盖 24/90 → 32/85）。
- 「事件分析」增持名单的股票名称改为可点击，跳转 `/stock-data?code=<代码>` 个股数据页，
  样式与持仓/自选一致（hover 变主色 + 下划线，title 提示"打开 XX 的个股数据"）；
  该行点击本身是展开/收起明细，链接上 `stopPropagation` 阻断冒泡，点名称不会误收起
  （无头浏览器实测：点击后行数 13→13，未触发展开）。

## 未发布（2026-09-03 上游对账移植）

- 资讯雷达移植条目层去重（investment-news v1.0.3）：URL 归一化剥 13 个白名单跟踪
  参数（urlencode 重转义防误合并）+ 标题 48h 窗口去重（无时间戳不去重、基准时间随
  保留条目刷新）；源清单去掉 Engadget / 少数派两个跨栏重复源（108→106）。
- 内嵌 a-stock-data 副本对齐上游 v3.7.1（筹码分布 CYQ、估值历史等 54 端点文档）。
- 上游对账：webapp 线遗留清零（v0.3.x 已于 2026-08-11 同步；GPU 租金信号页评估后
  不引入——海外算力租金数据与 A 股主线关联弱，恢复路径已记录）。上游 v1.0.x 已重写为
  Codex Harness Agent 产品并删除 backend/frontend，git merge 永久不可行。
  对账文档：`docs/upstream-sync/2026-09-03-v1.0.x-对账-webapp遗留移植.md`

## v1.8.0 — 2026-08-18

- 新增「因子研究」页：横截面选股因子的检验与探索性组合回测（研究型因子实验室，不做策略产品）。9 个价量因子（动量/反转/波动/下行波动/Amihud 非流动性/成交额稳定性），Alphalens 口径检验（RankIC/ICIR/IC 衰减/五分组收益/分组换手/因子秩自相关/分年表现）。
- 因子构建：公式引擎（字段 + 时序/截面算子，递归下降解析执行、无 eval），自定义因子保存后直接进检验与回测（`custom:` 前缀）。
- 因子库对标论文与业界实践扩至 41 个内置因子：新增 MAX 效应（Bali 2011）、52 周高点、青蛙效应（Wu 2022）、动量质量、隔夜/日内分解（LPS 2019）、特质波动（Ang 2006）、低贝塔、协偏度、非流动性冲击、Parkinson 高低价波动、量趋势等 20 个价量因子 + 12 个 PIT 财务因子（EP/SUE/应计/毛利率/ROE/成长/稳定性/ΔROE/利润含金量）；因子下拉按价量/财务/自定义分组。设计文档与缺失清单见 `research/factor_lab_factor_design.md`。
- PIT 财务数据层：东财业绩报表按报告期全量拉取（2006-今，含实际公告日），因子按「公告日 ≤ T 的最新报告期」取数（公告前不可见，杜绝前视）；`fn_*` 财务字段（营收/净利/EPS/ROE/同比/毛利率等）进公式引擎。
- 探索性组合回测：只多等权 TopN、周/月调仓、T 收盘出信号 T+1 开盘成交，佣金/印花税/滑点成本 0-3x 压力测试，净值 vs 全池等权基准、分年收益；一字板顺延、停牌近似处理。
- 数据层：全 A 日线（腾讯前复权，含深历史脏价格清洗）落 `~/.vibe-research/factor/`，后台构建 + 进度轮询；探索级数据边界（幸存者偏差/前复权/无 point-in-time 状态）随结果展示。
- MCP 新增 `factor_lab_status` / `factor_evaluate` / `factor_backtest`。

## v1.7.0 — 2026-08-17

- 新增「择时配置」页：宏观 × 流动性 × 市场确认合成择时分，5 档风险等级 + 风险预算倍率 + 现金底仓，输出股/债/商品/现金目标权重与调仓建议；四条门控规则制动，资产分缺失回退锚点组合。
- 行业研究新增「产业链纵深」：AI 算力与人形机器人两条示范链的环节图谱、各环节财务中位数（利润沉淀）与瓶颈/景气传导（`/api/industry-chains`）。
- 导航改为分组两级结构；「板块研究」更名「行业研究」。
- MCP 新增 `query_industry_chain` 与 `query_timing_allocation`。

## v1.6.0 — 2026-08-15

- 新增「油价」页：5 维 8 指标多维评分（EIA 库存/产量/炼厂投入、CFTC 净多头、地缘风险 GPR、美元指数、布伦特动量），含 Brent-WTI、SC 基差与裂解价差代理的结构层数据；EIA bulk zip 为主源，DEMO_KEY 限流自动退避。
- 「黄金」修复评分历史前视偏差：历史分位改为逐点扩展窗口（每个历史时点只用截至当日的数据），周末计算的分数落到最近交易日。
- 「债市」八状态框架支持 3 年逐月回放（宏观长序列补齐 OMO/社融存量/银行间隔夜），状态卡加迷你走势。
- MCP 新增 `query_oil_score` 油价多维评分工具。

## v1.5.0 — 2026-08-15

- 「债市」页上线：收益率曲线与期限利差矩阵、AAA 信用利差、Shibor 资金利率、LPR 政策锚、中债指数与中美利差，按「政策锚 → 资金 → 曲线 → 信用 → 全球」链条组织，聚合于 `/api/bonds/overview`；另提供八状态框架仪表盘、carry/roll/盈亏平衡计算层、国债期货仓位拥挤度与分品种评分（只呈现客观数据与确定性推导，不给利率走势判断）。
- MCP 工具层补齐研判类工具：宏观总分、流动性综合得分、行业评分、黄金评分与债市 6 件套（曲线/总览/框架/计算/仓位/分品种）。
- 研究归档新增中国债市研究框架 v1.1 与油价分析框架 v1.0。

## v1.4.1 — 2026-08-15

- 「标的筛选」个股分栏上线：一句话自然语言筛选，复用「问 AI」链路（用户自己的模型 + 后端数据工具）逐项核验候选，产出候选表与「未核验项与局限」；产品不内置候选池、不打分。
- MCP 工具描述补充中文示例调用，提升 Agent 端工具选择的准确率。

## v1.4.0 — 2026-08-15

- 「全球预期」新增 AI 研判：现状长条 + 核心模块研判卡（复用设置页「接入 AI」配置，未配置或失败自动降级为纯事件列表），支持单卡重生成（`/api/pulse/insight`）。
- 「宏观面」新增宏观总分卡：各模块之上的单一合成分，权重来自 2021–2026 逐月回测（回测脚本与明细见 `research/output/macro_backtest/`），含模块贡献分解与近 3 年总分走势。
- 「资金面」新增中美流动性综合得分卡：分指数加权合成、贡献条分解与近 3 年走势，点击分项可跳转对应指数。
- 「黄金」新增 PAXG 国内金价近似折算（新浪在岸 USDCNY，10 分钟缓存）与沪金主力近 1 年日 K 走势（`/api/gold/au0-hist`，1 小时缓存）；评分建议改为可操作话术。
- 导航顺序调整：全球预期紧跟市场全景，选举/AI 科技降为参考模块默认折叠。
- 新增「债市」页面占位（建设中），路由与导航已就位。

## v1.3.1 — 2026-08-14

- 重组中英文 README 的功能表，按实际独立入口逐项说明，避免合并无关模块。

## v1.3.0 — 2026-08-14

- 新增“全球预期”页面：整合 Polymarket 与 Kalshi 的公开概率数据，保留来源、刷新时间和历史走势。
- 新增“标的筛选”入口：基金筛选迁入统一页面，并预留个股筛选分栏。
- 完善黄金页面：补充现货行情、历史序列与数据状态展示；同步增强分钟图、汇率/市场展示和相关 API。

## v1.2.0 — 2026-08-12

- 新增 Manager-First 基金 PFS 初筛，明确区分公开数据证据、中性先验与尚未验证的能力指标。
- 股票和基金持仓改为账号隔离存储，并提供旧版数据检查与手动导入，避免不同账号共享台账。
- 为高成本数据统一加入 last-good 缓存、后台刷新、失败退避和来源时间展示；页面进入时先展示缓存，再自动更新。
- 完善持仓收益、基金观察与筛选、个股 K 线、市场复盘、宏观、流动性、黄金和板块评分交互。
- 加强用户数据版本控制、新闻与证券缓存，以及相关 API、隔离性和数据新鲜度测试。

## v1.1.0 — 2026-08-11

项目更名为 **Market Workbench**。更新网页标题、后端 API、MCP server、模型上下文和文档；保留原有 Git 仓库地址、launchd 标识及 `~/.vibe-research/` 数据目录，避免影响现有克隆、后台服务和本地数据。

## v1.0.1 — 2026-08-11

更新项目文档：重写中英文 README，补充本地启动、配置、架构与后端 API/MCP 说明。

## v1.0.0 — 2026-08-11

首个正式版本：整合宏观、资金面、板块、黄金、持仓、基金与研究工作流。

## v0.3.1 — 2026-08-09

三个用户报告的 bug + 版本号治理。感谢 [@lihaoran0412](https://github.com/lihaoran0412)
一口气提了三份带根因和文件行号的报告，质量很高。

### 修复：`query_market scope=turnover` 字段全为 null（#28）

`tools.py` 按 `turnover` / `changePct` 取字段，而 `astock.market_turnover_rank()`
实际返回的是 `price` / `pct` / `amount` / `mcap` / `float_cap` / `industry`——键名对不上，
每条只剩 `name` 和 `code`，其余一片空白。已对齐字段名，实测 20 条全部有值。

### 修复：Windows 下 MCP server stdout GBK 编码崩溃（#27）

Windows 上 Python 的 stdio 默认编码是 GBK(cp936)。JSON-RPC 响应里带中文、RSS 正文里的
`\xa0`（不换行空格）等字符 GBK 编不出来，**整条响应写不出去**——客户端表现为工具调用
失败 + 反复重连；即便不崩，中文也会被按 UTF-8 解 GBK 字节，全是乱码。

`mcp_server.main()` 现在在读写任何协议内容之前把 stdio 钉死成 UTF-8。选择重配而不是
退让成 `ensure_ascii=True`：后者能防崩，但会把中文全变成转义序列、体积翻几倍，而 MCP
协议本身就要求 UTF-8。

### 改进：证券搜索加备用端点，并区分「接口不可用」与「查无此票」（#26）

报告者的环境下美股/港股/韩股查询全部失败，而产品只回一句「未找到对应代码」，
他只能自己逆向排查到底哪一步坏了。

⚠️ **该接口从我们这边实测是正常的**（AAPL / 00700 / TSLA 均能解析），所以更可能是
IP 风控或链路问题，而非接口下线。但暴露出的两个真问题已修：

- **`except Exception: return None` 把两种情况压成一个返回值**——"这只票不存在"和
  "接口请求失败"从此不可区分。现在后者抛 `SearchUnavailable`，带上真实的底层错误，
  并明说「这与查无此代码是两回事」。
- **单一端点故障会让整块功能瘫痪**——新增备用端点 `searchadapter.eastmoney.com`，
  主端点失败自动切换。**并且必须校验响应结构再收手**：主端点返回「合法 JSON 但没有
  `QuotationCodeTable`」（接口改版 / 风控页 / HTTP 错误页，`em_get` 不做
  `raise_for_status`）时会被误当成"查得到但没匹配"，备用端点根本轮不上——而这恰恰
  就是本 issue 描述的情形，不校验的话这次修复等于没修。

### 修复：MCP `serverInfo` 版本号仍写死 `0.2.2`（#20 补漏）

#20 只列了 3 处硬编码，照着改会漏掉第 4 处：MCP 客户端初始化拿到的还是旧版本。
现抽出 `backend/version.py` 作为唯一读取点，四处同源。

**刻意独立成模块而不是从 `app` 导入**：`app.py` 在导入时会 `pf.start_scheduler(1800)`
起后台线程，MCP 服务只想拿个版本号，不该承担那个副作用。读取失败的警告走 **stderr**——
MCP 的 stdout 专供 JSON-RPC，往那儿打一行警告会插在初始化响应之前，客户端可能拒收整条流。

### 测试

`backend/` 90 passed（新增 11 例），含三条反向边界：GBK stdout 下不修就必崩（先证明坑
真实存在）、接口正常但查无此票仍返回 None、读不到版本号时 stdout 必须为空。

---

## v0.3.0 及更早

本文件自 v0.3.1 起维护；更早的版本历史见
[Releases](https://github.com/simonlin1212/Vibe-Research/releases)。
